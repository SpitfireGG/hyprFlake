$mainMod = SUPER
bind = $mainMod, Q, killactive 
bind = $mainMod, delete,exec,  hyprlock 
bind = $mainMod, W, togglefloating 
bind = ALT, return, fullscreen 
bindl = Alt, P,exec,   pavucontrol
bindl = ALT,S, exec, shutdown
bind = $mainMod, A, exec, ghostty
bind = $mainMod, G, exec, altus

bind = $mainMod, E, exec, thunar
bind = $mainMod, Y, exec, /home/archbishop/Dev/bash/dmenu_scripts/calc.sh
bind = $mainMod, F, exec, firefox-devedition
bind = $mainModShift,F , exec, firefox-devedition --private-window 
bind = $mainMod, T, exec, font-manager
bind = ControlShift, Escape, exec, gnome-system-monitor
bind = ControlShift, U, exec, alacritty -e  btop

$scratchpadsize = size 70% 70%
$scratchpad = class:^(scratchpad)$

bindl = Alt, delete, exec, shutdown --halt +1
bindl = ALT,R, exec, reboot


bind = $mainMod, backspace, exec, /home/archbishop/.config/rofi/powermenu/type-2/powermenu.sh     
bind = $mainMod, tab, exec, pkill rofi || ~/.config/hypr/scripts/rofilaunch.sh  


bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind=, XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+
bind=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-

bind= ControlShift,m , exec, swayosd --output-volume 0
bind= ControlShift,x , exec, swayosd --output-volume raise 2
bind= ControlShift,z, exec, swayosd --output-volume lower 2

bindl = ,F11, exec, brightnessctl set 1%-
bindl = ,F12, exec, brightnessctl set 1%+

bindl = ,XF86MonBrightnessUp,exec, brightnessctl set 1%+
bindl = ,XF86MonBrightnessDown,exec, brightnessctl set 1%-


bind = SuperShift, N, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"`
bind = SuperShift, B, exec, playerctl previous
bind = SuperShift, P, exec, playerctl play-pausekeybind


#bind = $mainMod, backspace, exec, wlogout --protocol layer-shell 
bind = $mainMod, P, exec, grim -g "$(slurp)" - | swappy -f - 
bind = $mainMod, J, exec, notify-send "Hyprland version: $(hyprctl version | head -2 | tail -1 | cut -f2 -d ' ')" "" -a 'Hyprland keybind'
bind = $mainMod, Y, exec, notify-send "Millis since epoch" "$(date +%s%N | cut -b1-13)" -a 'Hyprland keybind'
bind = SuperAlt, Equal, exec, notify-send "Hyprland test" "oh yeah this fucking works well" -u critical -a 'Hyprland keybind'
bind = , F9, exec, grim -g "$(slurp)" - | swappy -f - 

bind = SUPER, SPACE, exec,  noctalia-shell ipc call launcher toggle
bind = SUPER, I, exec,  noctalia-shell ipc call sidePanel toggle
bind = SUPER, comma, exec,  noctalia-shell ipc call settings toggle
bind = SUPER, X, exec,  noctalia-shell ipc call volume increase
bind = SUPER, Z, exec,  noctalia-shell ipc call volume decrease
bind = SUPER, M, exec,  noctalia-shell ipc call volume mute
bind = SUPER, V, exec,  noctalia-shell ipc call brightness decrease
bind = SUPER, B, exec,  noctalia-shell ipc call brightness increase
bind = SUPER, H, exec,  noctalia-shell ipc call launcher clipboard
bind = SUPER, C, exec,  noctalia-shell ipc call launcher calculator
bind = SUPER, L, exec,  noctalia-shell ipc call lockScreen  lock
bind = SUPER, R, exec,  noctalia-shell ipc call plugin:assistant-panel toggle
bind  = SUPER , K , exec , noctalia-shell ipc call plugin:assistant-panel setModel "gpt-4o-mini"
bind  = SUPER , G , exec , noctalia-shell ipc call plugin:assistant-panel setModel "gemini-2.5-flash"
  

